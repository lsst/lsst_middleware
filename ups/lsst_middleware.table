# Core middleware packages.
setupRequired(daf_butler)
setupRequired(pipe_base)
setupRequired(ctrl_mpexec)
setupRequired(ctrl_bps)

# BPS plugins.
setupRequired(lsst_bps_plugins)

# Middleware packages that provide additional
# functionality without depending on pipeline
# C++ packages.
setupRequired(dax_obscore)
setupRequired(daf_butler_migrate)
